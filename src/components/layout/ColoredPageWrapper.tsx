import React, { useState, useEffect } from "react"; import { pickPantone } from "@/styles/pantoneBackgrounds"; interface ColoredPageWrapperProps { children: React.ReactNode; seed?: string; } const ColoredPageWrapper: React.FC<ColoredPageWrapperProps> = ({ children, seed }) => { const [bgColor, setBgColor] = useState<string>(""); useEffect(() => { const color = pickPantone(seed); setBgColor(color); }, [seed]); return ( <div className="relative w-full min-h-screen pb-8" style={{ backgroundColor: bgColor || "#534540" }}> <div className="absolute inset-0 bg-black/40 z-0 pointer-events-none" /> <div className="relative z-10">{children}</div> </div> ); }; export default ColoredPageWrapper;
